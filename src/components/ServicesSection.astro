---
import { getTranslations } from '../i18n/utils';

interface Props {
  lang: 'cs' | 'en';
}

const { lang } = Astro.props;
const t = getTranslations(lang);

const services = lang === 'cs' ? [
  {
    title: 'Flutter vývoj aplikací',
    description: 'Multiplatformní mobilní aplikace s offline-first architekturou, PDF fakturací a QR platbami.',
    href: '/cs/services/vyvoj-mobilnich-aplikaci',
    icon: '📱',
    tags: ['Android', 'iOS', 'Offline-first'],
  },
  {
    title: 'AI audit webu (GEO)',
    description: 'Audit viditelnosti v AI vyhledávačích. Zjistěte, jak ChatGPT a další AI vidí váš web.',
    href: '/cs/services/ai-audit-webu',
    icon: '🔍',
    tags: ['ChatGPT', 'Perplexity', 'SEO'],
  },
  {
    title: 'Web Firewall skenování',
    description: 'Detekce manipulativních vzorů, prompt injection a dark patterns na webových stránkách.',
    href: '/cs/services/web-firewall-skenovani',
    icon: '🛡️',
    tags: ['Bezpečnost', 'Dark patterns', 'AI safety'],
  },
] : [
  {
    title: 'Flutter App Development',
    description: 'Cross-platform mobile apps with offline-first architecture, PDF invoicing, and QR payments.',
    href: '/en/services/flutter-app-development',
    icon: '📱',
    tags: ['Android', 'iOS', 'Offline-first'],
  },
  {
    title: 'AI Visibility Audit (GEO)',
    description: 'Audit your visibility in AI search. Find out how ChatGPT and other AI see your website.',
    href: '/en/services/ai-visibility-audit',
    icon: '🔍',
    tags: ['ChatGPT', 'Perplexity', 'SEO'],
  },
  {
    title: 'Web Firewall Scanning',
    description: 'Detection of manipulative patterns, prompt injection, and dark patterns on websites.',
    href: '/en/services/web-firewall-scanning',
    icon: '🛡️',
    tags: ['Security', 'Dark patterns', 'AI safety'],
  },
];

const sectionTitle = lang === 'cs' ? 'Služby' : 'Services';
const sectionSubtitle = lang === 'cs' 
  ? 'Pomáhám firmám a jednotlivcům s mobilními aplikacemi, AI viditelností a webovou bezpečností.'
  : 'I help businesses and individuals with mobile apps, AI visibility, and web security.';
const viewAllLabel = lang === 'cs' ? 'Zjistit více' : 'Learn more';
---

<section id="services" class="py-20 scroll-mt-20">
  <div class="container-main">
    <div class="mb-12">
      <h2 class="section-heading">{sectionTitle}</h2>
      <p class="section-subheading">{sectionSubtitle}</p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {services.map((service) => (
        <a 
          href={service.href}
          class="glass-card p-6 group hover:border-brand-500/50 transition-all duration-300"
        >
          <div class="text-4xl mb-4">{service.icon}</div>
          
          <h3 class="text-xl font-display font-semibold text-white mb-3 group-hover:text-brand-400 transition-colors">
            {service.title}
          </h3>
          
          <p class="text-surface-400 mb-4 text-sm">
            {service.description}
          </p>
          
          <div class="flex flex-wrap gap-2 mb-4">
            {service.tags.map((tag) => (
              <span class="tag text-xs">{tag}</span>
            ))}
          </div>
          
          <span class="inline-flex items-center text-brand-400 text-sm font-medium group-hover:text-brand-300 transition-colors">
            {viewAllLabel}
            <svg class="w-4 h-4 ml-1 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </span>
        </a>
      ))}
    </div>
  </div>
</section>
